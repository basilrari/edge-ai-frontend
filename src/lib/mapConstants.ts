/** Leaflet zoom level — ~15–20 m across a typical panel at lat ~25°. */
export const MAP_MAX_ZOOM = 22;

/** Esri native imagery stops around 19; allow overzoom for closer inspection. */
export const SATELLITE_MAX_NATIVE_ZOOM = 19;
